spring:
  activemq:
    broker-url: ${ACTIVEMQ_BROKER_URL}
    user: ${ACTIVEMQ_USER}
    password: ${ACTIVEMQ_PASSWORD}
  data:
    mongodb:
      uri: ${MONGODB_URI}

logging:
  level:
    root: INFO
    org.springframework: INFO
    com.gym.crm: INFO