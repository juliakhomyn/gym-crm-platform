spring:
  application:
    name: workload-service

server:
  port: 8081
  servlet:
    context-path: /workload-service

app:
  api:
    base-path: /api/v1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/