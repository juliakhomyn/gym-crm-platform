spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  liquibase:
    changelog: classpath:db/test-migration/master.xml

app:
  api:
    base-path: /api/v1
  cors:
    allowed-origins: http://localhost:3000

jwt:
  secret: TestSecret777mtsbW5vcHFyc3R1dnd4eXo1Njc4OTAxMjM0NTY3OA==
  expiration: 3600000