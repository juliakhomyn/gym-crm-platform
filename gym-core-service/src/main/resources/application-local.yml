spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym_db
    username: gymuser
    password: gympass
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    org.springframework: INFO
    com.gym.crm: DEBUG